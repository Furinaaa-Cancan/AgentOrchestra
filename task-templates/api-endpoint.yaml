id: api-endpoint
name: "API Endpoint"
description: "实现单个 API endpoint"
requirement: "${method} ${path} — ${description}。要求：1) 实现路由处理函数 2) 输入验证（Pydantic schema 或等效）3) 错误处理和合适的 HTTP 状态码 4) 单元测试 5) 更新 API 文档。"
skill: code-implement
decompose: false
tags: [backend, api]
variables:
  method: "POST"
  path: "/api/resource"
  description: "请通过 --var description='...' 指定 endpoint 功能"
